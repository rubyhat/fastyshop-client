import { Container, Grid } from "@mui/material";

export const ProfileDetailsModule = () => {
  return (
    <Container maxWidth={false}>
      <Grid container spacing={2}>
        <Grid size={12}>ProfileDetailsModule</Grid>
      </Grid>
    </Container>
  );
};

import { Container, Grid } from "@mui/material";

export const OrdersModule = () => {
  return (
    <Container maxWidth={false}>
      <Grid container spacing={2}>
        <Grid size={12}>Orders module</Grid>
      </Grid>
    </Container>
  );
};

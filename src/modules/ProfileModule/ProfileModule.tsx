import { Container, Grid } from "@mui/material";

export const ProfileModule = () => {
  return (
    <Container>
      <Grid container spacing={2}>
        Profile module init
      </Grid>
    </Container>
  );
};
